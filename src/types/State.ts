export interface CurrentStateGet {
  chapterTitle: string;
  chapterPosition: string;
  hasValue?: boolean;
}

export interface CurrentStateSet {
  chapterTitle: string;
  chapterPosition: string;
}
